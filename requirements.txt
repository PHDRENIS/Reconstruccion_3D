# TT Limpio - requirements unificados (FV2 + F Vision + depth_completion)
# Core
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=10.0.0
pyyaml>=6.0
tqdm>=4.65.0
loguru>=0.7.0

# Deep Learning - YOLO / Fusion
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.3.0

# Depth Completion (TensorFlow)
tensorflow>=2.15.0
# Si usas GPU: tensorflow[and-cuda] o tf-nightly

# 3D / Vision
open3d>=0.17.0
scikit-learn>=1.3.0
scipy>=1.11.0
matplotlib>=3.7.0

# Utils
orjson>=3.9.0
scikit-image>=0.21.0

# Opcional - HPC / notebooks
# jupyter>=1.0.0
# ipywidgets>=8.0.0
# plotly>=5.0.0
