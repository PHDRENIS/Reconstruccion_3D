# Legacy configs — Fusion Vision v1

Configs originales con rutas `/zfs-home/202501029N/...` y `C:\Users\victo\...` movidas aquí como referencia.
No se usan en el pipeline actual (FV2). Ver `configs/fv2_config.yaml` y `configs/yolo_ir_config.yaml`.
