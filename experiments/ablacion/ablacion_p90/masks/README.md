# Masks ablacion

Masks generadas por YOLO-seg (3x 318 imgs). Eliminadas del repo para bloat (>950 archivos, ~4.6 MB).
Regenerar con python src/segmentation/main.py --config configs/yolo_ir_config.yaml o desde outputs.
Solo segmentation_summary.txt se versiona.
